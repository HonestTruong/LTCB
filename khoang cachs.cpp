#include <stdio.h>
#include <math.h>
#include <limits.h>
#definr MAX 100

int main(){
	int x, a[MAX], n, i, max;
	max = INT_MIN;
	scanf("%d", &n);
	for(i=0; i<n; i++)
		scanf("%d", &a[i]);
	scanf("%d", &x);
	for(i=0; i<n; i++)
	{
		if()
	}
}
